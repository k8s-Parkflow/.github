name: "PR Title Auto Format"

on:
  pull_request:
    types: [opened, edited, synchronize]

jobs:
  call-workflow:
    # 'k8s-Parkflow/.github' 레포지토리에 있는 파일을 호출함
    uses: k9s-Parkflow/.github/workflows/reusable-pr-title.yml@main
